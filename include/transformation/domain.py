from typing import Any, Tuple
from include.helpers.data_storage import MinIOStorage


def transform_currencies(storage: MinIOStorage) -> str:
    # first read data from minio
    

    # then extract and format the timestamp

    # then structure the data as {"timestamp" : (values)}

    # then write the data to minio

    # return the path to the file in minio
    pass